services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=courses'
      - 'MYSQL_PASSWORD='
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=root'
    ports:
      - '3306'